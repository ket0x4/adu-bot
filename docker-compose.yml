services:
  bot:
    build: .
    container_name: adu_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/adu_bot.db
      - LOG_PATH=/app/data/adu_bot.log
    volumes:
      - ./data:/app/data
